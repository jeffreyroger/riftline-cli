"""Defines compute() -- the real home of this symbol.

re-exported as reexport_pkg.compute via reexport_pkg/__init__.py.
"""


def compute(x):
    """Square x.  Single-level re-export target."""
    return x * x
